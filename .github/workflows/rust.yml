name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust: [nightly]

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: cargo build --verbose
