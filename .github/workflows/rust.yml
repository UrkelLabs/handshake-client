name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust: [nightly]

    steps:
    - name: Build
    - uses: hecrj/setup-rust-action@v1.1.0
      run: cargo build --verbose
